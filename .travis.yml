dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.6"
  - "3.6.1"
  - "3.7"

# command to install dependencies
install:
    - pip install .
# command to run tests
script: pytest
